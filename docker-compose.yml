services:
  app:
    build:
      context: .
      args:
        NPM_REGISTRY: https://package-mirror.liara.ir/repository/npm/
    container_name: barmax_next
    restart: unless-stopped
    ports:
      - "127.0.0.1:3000:3000"
    env_file:
      - .env.production
